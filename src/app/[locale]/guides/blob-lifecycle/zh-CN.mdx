import UploadBlobTs from './_snippets/upload-blob-ts.mdx'

export const header = {
  title: 'Blob Lifecycle',
  description: 'How blobs are uploaded, referenced, and deleted',
}

## 上传 blob

必须先将 Blob 上传到 PDS，然后才能创建引用该 Blob 的记录。请注意，服务器在接收上传时并不知道预期的 Lexicon，因此只能在初始上传时应用通用 blob 限制和限制，然后在创建记录时强制执行 Lexicon 定义的限制。

客户端在其 PDS 上使用 **`com.atproto.repo.uploadBlob`** 端点，该端点将以 Lexicon blob 对象的形式返回经过验证的元数据。客户端应设置 HTTP ** `Content-Type`** 标头，并应在上传请求上设置 ** `Content-Length`** 标头。 SDK 可以自动处理这个问题：

<UploadBlobTs />

然后可以在另一个记录中引用这个“数据”对象，例如作为“嵌入”。

上传也可以允许分块传输编码。服务器*可能*嗅探 blob mimetype 以根据声明的 **`Content-Type`** 标头进行验证，并在响应中返回修改后的 mimetype，或拒绝上传。请参阅[“安全注意事项”](/specs/blob#security-considerations)。如果实际的 blob 上传大小与 ** `Content-Length`** 标头不同，服务器应拒绝上传。

## 垃圾收集

成功上传后，Blob 将被放置在临时存储中。在此状态下无法下载或分发它们。服务器应该在适当的时间跨度后“垃圾收集”（删除）未引用的临时 blob（请参阅[实现指南](#web-accessibility)）。临时存储中的 Blob 不应包含在 **`listBlobs`** 输出中。

## 引用 blob

现在可以通过将返回的 blob 元数据包含在记录中来从记录中引用上传 blob。在处理记录创建时，服务器提取所有引用的 blob 集合，并检查它们是否已被引用或位于临时存储中。一旦记录创建成功，服务器就会使该 blob 可供公开访问。

同一存储库中的多个记录可以引用相同的 blob。重新上传已存储和引用的 Blob 不会导致现有 Blob 或记录发生任何更改。

创建引用不存在的 blob 的新单独记录的创建应在创建（或更新）时被拒绝。但是，服务器可以托管引用本地不可用的 blob 的存储库记录。例如，在批量存储库导入或帐户迁移期间；数据丢失；或出于政策原因删除/删除内容。

## 删除斑点

当删除引用 blob 的记录时，服务器会检查同一存储库中是否有任何其他当前记录引用该 blob。如果不是，则该 blob 将与记录一起删除。

删除帐户后，所有托管的 blob 都会在合理的时间范围内删除。当帐户被停用、删除或暂停时，Blob 不应公开访问。

服务器可能会决定使单个 blob 无法访问，与任何帐户删除或其他帐户生命周期事件分开。

## 网络可访问性

可以使用 **`com.atproto.sync.getBlob`** 端点从 PDS 获取原始 blob。服务器应返回适当的 ** `Content-Type`** 和 ** `Content-Length`** HTTP 标头。直接从 PDS 向最终用户浏览器提供媒体不是推荐或必需的模式，并且服务器不需要支持或促进此用例。有关详细信息，请参阅“安全注意事项”。

服务器可能有自己的通用限制和 blob 策略，与 Lexicon 定义的任何约束分开。他们可能会对数据存储实施帐户范围的配额；最大斑点尺寸；内容政策；等等。任何这些限制都可能在初始上传时强制执行。服务器操作员应该意识到限制和其他限制可能会影响现有和未来应用程序的功能。为了最大限度地提高互操作性，建议运营商优先考虑对总体帐户资源消耗进行限制（例如，“总 blob 大小”配额，而不是“每个 blob”大小限制）。

某些应用程序在 Blob 上传和记录引用之间可能存在较长的延迟。为了最大限度地提高互操作性，建议服务器实现和操作员在“垃圾收集”之前留出几个小时的宽限时间，至少一小时是固定的下限。

## 相关资源

- [图像和视频](/guides/images-and-video)
- [Blob 安全](/guides/blob-security)
- [视频处理](/guides/video-handling)
- [Blob 规格](/specs/blob)
- [Streamplace](https://stream.place/docs/) 提供了 atproto 上视频流的实现和相关 Lexicon。